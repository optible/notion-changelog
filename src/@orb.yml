version: 2.1

description: >
  This will generate changelog for notion which can be later added to the release note that's generated. It will convert all the ticket to the urls.

# This information will be displayed in the orb registry and is not mandatory.
display:
  home_url: 'https://github.com/optible/notion-changelog'
  source_url: 'https://github.com/optible/notion-changelog'
# If your orb requires other orbs, you can import them like this. Otherwise remove the "orbs" stanza.
# orbs:
#  hello: circleci/hello-build@0.0.5
